NOTION_API_KEY=<notion-integration-secret>
NOTION_VERSION=2026-03-11

TIDB_HOST=<tidb-host>
TIDB_PORT=4000
TIDB_USER=<tidb-sync-writer-user>
TIDB_PASSWORD=<tidb-password>
TIDB_DATABASE=notion_context
TIDB_ENABLE_SSL=true
TIDB_CA_PATH=

# Optional: keep personal Notion page IDs/titles in .env instead of
# mirror.config.json. This value takes precedence when present.
MIRROR_CONFIG_JSON={"pages":[{"pageId":"xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx","title":"Example Context Page"}]}

# Absolute path to a local editor-knowledge library. The commands
# use a fixed allowlist of 8 Markdown paths below this root.
EDITOR_KNOWLEDGE_SOURCE_ROOT=<absolute-path-to-editor-knowledge-library>

# Absolute path whose children include startup-science/ and marketing-wisdom/.
# Only the two fixed Markdown paths allowlisted in code are read.
BUSINESS_KNOWLEDGE_SOURCE_ROOT=<absolute-path-to-business-knowledge-library>

# Absolute path to a local author-style library. Only the two fixed Markdown paths in
# knowledge/ are read by the dedicated author-style commands.
AUTHOR_STYLE_SOURCE_ROOT=<absolute-path-to-author-style-library>

# Absolute path to a local metaskill library. The dedicated commands read only
# the fixed Markdown path allowlisted in code below this root.
METASKILL_SOURCE_ROOT=<absolute-path-to-metaskill-library>
